# Backend Audit — Performance (EF Core) — DsaVisual

- Phạm vi: `backend/src/DsaVisual.Application/Services/*` (13 service) + `backend/src/DsaVisual.Api/Controllers/*` + `backend/src/DsaVisual.Application/Persistence/Configurations/*`
- Ngày: 2026-08-13 · Nhánh: feature/backend-audit (ghi đè toàn bộ file cũ)
- Phương pháp: đọc code tĩnh (không chạy build); đối chiếu query ↔ index Configuration.
- Ghi chú chung TỐT: hầu hết query đọc đã dùng `AsNoTracking()` đúng chỗ; các điểm join/batch lớn (ProgressService.GetReportAsync, GetMyLessonAsync, GetLearningPathAsync, ClassService.GetMyClassesAsync, GamificationService.GetShopItemsAsync/GetQuestsAsync) đã viết đúng dạng batch + projection — không phát hiện query đọc thiếu AsNoTracking.

| # | file:dòng | mức | mô tả | khuyến nghị |
|---|-----------|-----|-------|-------------|
| 1 | src/DsaVisual.Application/Services/ClassService.cs:124-147 | CAO | **N+1**: `GetByIdAsync` — `foreach (var assignment in assignments)` (dòng 124) gọi 1-2 query riêng `db.Lessons...FirstOrDefaultAsync` / `db.Exercises...FirstOrDefaultAsync` (dòng 129-136) cho mỗi assignment → N assignment = N-2N round-trip DB. | Gộp thành 2 query batch: `lessonIds` → `db.Lessons.Where(l => lessonIds.Contains(l.Id)).ToDictionary(l => l.Id, l => l.Title)` + tương tự `exerciseIds`; map trong memory (ClassAssignments có index LessonId/ExerciseId sẵn). |
| 2 | src/DsaVisual.Application/Services/ClassService.cs:450-475 | CAO | **N+1**: `GetReportAsync` — `foreach (var assignment in assignments)` (dòng 451) query title từng assignment (dòng 459-463). Với lớp có M bài gán → M round-trip; kết hợp load toàn bộ submissions ở #8. | Tách 2 query batch title (lessonIds/exerciseIds → ToDictionary) trước vòng lặp; hoặc join thẳng trong query submissions. |
| 3 | src/DsaVisual.Application/Services/ProgressService.cs:220-223 | CAO | **Query quá rộng + nguy cơ 500**: `LoadCountsAsync` — `db.UserProgress.Where(p => lessonIds.Contains(p.LessonId)).ToListAsync()` (dòng 220-222) KHÔNG filter `UserId` → load progress của **toàn bộ người dùng** cho mọi bài học chỉ để tính overview của 1 user (mỗi lần GET /progress/overview). Ngoài ra `ToDictionary(p => p.LessonId)` (dòng 223) sẽ ném `ArgumentException` nếu ≥2 user có progress cùng lesson (dữ liệu thật chắc chắn gặp) → request trả 500. | Thêm `p.UserId == userId` vào WHERE (đã có index (UserId, LessonId) unique); hoặc đẩy hẳn aggregation xuống SQL (GROUP BY LessonId). Đây là bug đúng-tính-năng + perf. |
| 4 | src/DsaVisual.Application/Services/GamificationService.cs:433-436 | TRUNG | **Pagination offset (leaderboard)**: `GetLeaderboardAsync` — `CountAsync` (433) + `OrderByDescending(Xp).ThenBy(Id).Skip().Take()` (434-438). Trang càng sâu càng chậm; đồng thời `ORDER BY Xp` không có index (xem #5) → sort toàn bộ Users mỗi lần. | Keyset/cursor: `WHERE (Xp < @lastXp) OR (Xp = @lastXp AND Id < @lastId) ORDER BY Xp DESC, Id DESC LIMIT n`; giữ CountAsync riêng cho total (hoặc bỏ total nếu FE chỉ cần "next"). |
| 5 | src/DsaVisual.Application/Services/GamificationService.cs:401,435 | TRUNG | **Thiếu index cho leaderboard**: query `WHERE DeletedAt IS NULL AND Role != Admin ORDER BY Xp DESC` — Configuration Users (UserConfiguration.cs:26-28) chỉ có index (Email), (Role, IsActive), (PremiumUntil) — KHÔNG có index theo Xp → SQL Server phải sort toàn bộ bảng mỗi request leaderboard. | Thêm index có điều kiện: `builder.HasIndex(u => u.Xp)` hoặc covering `(Role, Xp DESC)`; cân nhắc `HasFilter("[DeletedAt] IS NULL")`. |
| 6 | src/DsaVisual.Application/Services/ExerciseService.cs:790-803 | TRUNG | **Pagination offset (lịch sử bài làm)**: `QuerySubmissionsAsync` — `CountAsync` (790) + `OrderByDescending(SubmittedAt).Skip().Take()` (791-803). ExerciseSubmissions là bảng append-only tăng không ngừng; trang sâu chậm. | Keyset theo `SubmittedAt DESC, Id DESC`: `WHERE (SubmittedAt < @last) OR (SubmittedAt = @last AND Id < @lastId)`; đã có index (UserId, ExerciseId, SubmittedAt) phù hợp. |
| 7 | src/DsaVisual.Application/Services/ExerciseService.cs:832-845 | TRUNG | **Pagination offset (code submissions)**: `QueryCodeSubmissionsAsync` — cùng pattern CountAsync + Skip/Take trên CodeSubmissions (append-only). | Keyset như #6; index (UserId, ExerciseId, SubmittedAt) đã có. |
| 8 | src/DsaVisual.Application/Services/ClassService.cs:443-488 | TRUNG | **Load toàn bộ submissions rồi tính in-memory**: `GetReportAsync` — `db.ExerciseSubmissions...ToListAsync()` (444-448) tải mọi bài nộp của cả lớp (kèm AnswersJson/ResultJson JSON lớn) chỉ để đếm OnTime/Late/NotSubmitted/Avg (450-474). Bảng nộp bài tăng nhanh → tốn RAM + network. | Projection chỉ lấy cột cần: `Select(s => new { s.UserId, s.ClassAssignmentId, s.SubmittedAt, s.Score })` (không kéo AnswersJson/ResultJson); hoặc aggregate trong SQL: `GroupBy(ClassAssignmentId)` + `COUNT/AVG`. |
| 9 | src/DsaVisual.Api/Controllers/AdminController.cs:30-48 | TRUNG | **13 query riêng lẻ**: `GetStats` — 13 × `CountAsync` tuần tự (Users×4, Topics, Lessons, Exercises, ExerciseSubmissions, CodeSubmissions, Classes, Favorites, LessonSimulations, Users-active-today) = 13 round-trip DB cho 1 endpoint. | Gộp thành 1-2 query: `SELECT COUNT(...) AS Total, SUM(CASE WHEN Role=0 THEN 1 ELSE 0 END) AS Students, ... FROM Users WHERE DeletedAt IS NULL` + tương tự cho bảng còn lại (hoặc 1 query UNION ALL). |
| 10 | src/DsaVisual.Application/Services/CodeRunnerService.cs:89-93 | TRUNG | **Pagination in-memory trên JSON**: `GetTraceAsync` — `ParseTrace(run.TraceJson)` deserialize **toàn bộ** trace (có thể hàng nghìn event) rồi mới Skip/Take trong memory; trace càng lớn, mỗi request đọc trace càng tốn CPU/RAM. | Lưu trace dạng bảng con (TraceEvents) để phân trang SQL; hoặc giới hạn độ dài TraceJson khi save (SaveRunAsync:43); hoặc phân trang bằng JsonDocument streaming (chỉ parse 1 trang). |
| 11 | src/DsaVisual.Application/Services/GamificationService.cs:422-426 | TRUNG | **IN-list từ bảng con**: `GetLeaderboardAsync` tab "class" — `memberIds = ClassMembers...ToListAsync()` (422-425) tải toàn bộ member id về ứng dụng rồi `memberIds.Contains(u.Id)` (426) → IN list có thể lớn (lớp đông) + 2 round-trip. | Dùng `query.Where(u => db.ClassMembers.AsNoTracking().Any(m => m.ClassId == classId && m.UserId == u.Id))` — EF dịch thành EXISTS, 1 round-trip; đã có index (ClassId, UserId). |
| 12 | src/DsaVisual.Application/Services/LessonService.cs:95-99 | THAP | **Cartesian product (Include 2 collection)**: `GetByIdAsync` — `Include(l => l.LessonSimulations)` + `Include(l => l.Exercises...)` → 1 JOIN × 2 collection (lesson × sims × exercises). Volume/lesson nhỏ nên ít ảnh hưởng, nhưng đúng pattern cần tránh. | Tách 2 query (hoặc dùng Projection sang DTO như GetListAsync đã làm). |
| 13 | src/DsaVisual.Application/Services/LessonService.cs:162-165 | THAP | **Cartesian product (Include 2 collection)**: `UpdateAsync` — cùng pattern Include LessonSimulations + Exercises (cần cho ToDto sau update). | Không cần Include: sau update chỉ cần map DTO từ entity gốc + 2 query nhỏ riêng (hoặc dùng Projection). |
| 14 | src/DsaVisual.Application/Services/ExerciseService.cs:52-55 | THAP | **Pagination offset**: `GetListAsync` — CountAsync + Skip/Take trên Exercises (danh sách hiện nhỏ; đã Projection đúng). | Keyset theo Id khi danh sách lớn; hiện chấp nhận được. |
| 15 | src/DsaVisual.Application/Services/UserService.cs:43-46 | THAP | **Pagination offset**: `GetListAsync` (admin user list) — CountAsync + Skip/Take. | Keyset theo Id khi user > vài nghìn; hiện chấp nhận được. |
| 16 | src/DsaVisual.Application/Services/LessonService.cs:61-64 | THAP | **Pagination offset**: `GetListAsync` — CountAsync + Skip/Take; thêm subquery projection `db.UserProgress...FirstOrDefault()` trong Select (76-84) — EF dịch thành correlated subquery (1 SQL), chấp nhận được. | Keyset theo (SortOrder, Id) khi cần; subquery hiện OK. |
| 17 | src/DsaVisual.Application/Services/FavoriteService.cs:26-39 | THAP | **Loop gọi service trong foreach**: `GetListAsync` — `foreach (var favorite in favorites)` gọi `catalog.GetByKeyAsync` mỗi lượt. KHÔNG phải N+1 DB (catalog là dictionary in-memory, SimulationCatalogService.cs:21), chỉ là overhead vòng lặp. | Map 1 lần: `catalog.GetListAsync()` → ToDictionary(key) rồi lookup; hoặc giữ nguyên (chi phí thấp). |
| 18 | src/DsaVisual.Application/Services/GamificationService.cs:794-799 | THAP | **Loop gọi service trong foreach**: `RunBenchmarkAsync` — `foreach (var key in request.Keys)` gọi `catalog.GetByKeyAsync` mỗi lượt (in-memory, không phải N+1 DB). | `catalog.GetListAsync()` 1 lần → ToDictionary(key); chi phí thấp. |
| 19 | src/DsaVisual.Application/Services/GamificationService.cs:411 | THAP | **Thiếu index**: leaderboard tab "week" filter `u.LastActivityDate >= weekStart` — Users không có index trên LastActivityDate → scan. | Index `(LastActivityDate)` hoặc gộp vào index leaderboard (#5). |
| 20 | src/DsaVisual.Application/Persistence/Configurations/LessonConfiguration.cs:55-56 | THAP | **Thiếu index cho lesson list**: `GetListAsync` filter `(DeletedAt == null, Status == Active)` + `OrderBy(SortOrder, Id)` — chỉ có index (TopicId), (CreatedBy, Status). Bảng Lessons nhỏ nên ít ảnh hưởng. | Index `(Status, DeletedAt, SortOrder)` nếu Lessons phát triển. |
| 21 | src/DsaVisual.Api/Controllers/FeedbackController.cs:33-41 | THAP | **Tính aggregate in-memory**: `GetSummary` — tải toàn bộ `ContentFeedback` của lesson rồi `Average/Count` trong memory; nên tính trong SQL. | `db.ContentFeedback.Where(f => f.LessonId == lessonId).GroupBy(f => f.LessonId).Select(g => new { Avg = g.Average(f => f.Rating), Count = g.Count() })`. |
| 22 | src/DsaVisual.Application/Services/GamificationService.cs:181,202 | THAP | **Đọc/ghi trùng lặp**: `EnterNodeAsync` — `PersistHeartRegenAsync` (181) rồi `GetCurrentHeartsAsync` (202) gọi lại `PersistHeartRegenAsync` (874) → đọc Users 2 lần + có thể 2 UPDATE cùng lô trong 1 request. | Truyền user đã đọc qua tham số hoặc đọc 1 lần rồi tái sử dụng; tách khỏi transaction khi có thể. |
| 23 | src/DsaVisual.Application/Services/TopicService.cs:219-228 | THAP | **Load toàn bộ siblings mỗi lần**: `SiblingNameExistsAsync` — tải hết topics cùng ParentId về memory để so sánh tên (do collation), gọi mỗi Create/Update topic. Bảng nhỏ nên chấp nhận được. | Giữ nguyên (chủ đích tránh lệch collation) hoặc dùng `EF.Functions.Collate` + `AnyAsync` khi cần scale. |

---

## Tổng kết

- **Tổng số findings: 23**
- **CAO: 3** (#1, #2 — N+1 ClassService; #3 — ProgressService query quá rộng + nguy cơ 500)
- **TRUNG: 8** (#4-#11)
- **THAP: 12** (#12-#23)

## Ưu tiên xử lý

1. **#3 (ProgressService.LoadCountsAsync)** — vừa lỗi perf vừa lỗi đúng-tính-năng (ToDictionary duplicate key → 500) — sửa ngay: thêm filter `UserId`.
2. **#1, #2 (ClassService N+1)** — chuyển foreach query → 2 batch query; dễ sửa, ảnh hưởng trực tiếp GET class detail + class report.
3. **#5, #4 (leaderboard)** — thêm index Xp + keyset pagination; leaderboard là endpoint đọc nhiều nhất của hệ gamification.
4. **#6, #7, #8, #9, #10** — pagination keyset cho submissions/code-submissions + giảm round-trip stats + projection gọn report.
