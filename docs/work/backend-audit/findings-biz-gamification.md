# FINDINGS — Audit Business Logic GamificationService (trục XP/gems/hearts/premium/quests/shop/leaderboard)

> Ngày: 13/08/2026 · Phạm vi: `backend/src/DsaVisual.Application/Services/GamificationService.cs` (1100 dòng), entities, GamificationConfiguration.cs, GamificationController.cs + rà soát toàn repo các điểm ghi `Users`/`UserQuests`. CHỈ AUDIT — không sửa code.
> Nguồn: PROMPT_J_BACKEND_AUDIT §2. BUG-1 đã biết trước (docs/work/audit-notes-backend.md), ghi nhận đầu bảng.

| # | file:dòng | mức | mô tả | khuyến nghị |
|---|-----------|-----|-------|-------------|
| BUG-1 | GamificationService.cs:840-844 (+713) | CAO | HeartConfig() trả `user.HeartsMax` (DB vẫn 30) khi hết hạn premium — KHÔNG có job/hosted service downgrade (toàn repo không có BackgroundService/IHostedService). SRS FR-10.7: clamp HeartsMax 30→10, Hearts=MIN(Hearts,10), regen 30p/tim. MockPay chỉ set HeartsMax=30 một chiều, không có nhánh trả về. | Thêm hosted job (hoặc lazy-check tại HeartConfig/PersistHeartRegen) khi `PremiumUntil <= now`: cập nhật atomic `SET HeartsMax=10, Hearts=CASE WHEN Hearts>10 THEN 10 ELSE Hearts END`, reset LastHeartAt nếu cần; kèm migration data + test hết hạn. |
| 2 | GamificationService.cs:867-868 (PersistHeartRegenAsync 851-869) | CAO | UPDATE regen tim KHÔNG điều kiện: `SET Hearts = {computed}, LastHeartAt = {now} WHERE Id = userId` — computed từ read AsNoTracking stale. Race: GetHeartsAsync/GetCurrentHeartsAsync (chạy ngoài tx) song song EnterNodeAsync → ghi đè kết quả `Hearts = Hearts - 1` vừa commit → user được "trả lại" tim đã trừ (lost-update); ngược lại 2 regen song song có thể ghi giá trị cũ. Vi phạm yêu cầu (a): hearts không âm/không sai sau thao tác song song. | Dùng 1 UPDATE atomic: `UPDATE Users SET Hearts = Hearts + {delta}, LastHeartAt = {now} WHERE Id=@id AND Hearts < {maxHearts}` (delta tính trong SQL, clamp bằng CASE), hoặc `SET Hearts = CASE WHEN Hearts < {computed} THEN Hearts + {regenCount} ELSE Hearts END, LastHeartAt = CASE ... END WHERE Id=@id AND LastHeartAt <= {lastHeartAtRead}`; + concurrency token (xem #3). |
| 3 | User.cs:4-28 (toàn repo: 0 file dùng RowVersion/IsConcurrencyToken/[Timestamp]) | CAO | KHÔNG có concurrency token cho Users (và UserInventory/PremiumSubscriptions/UserQuests/UserNodeProgress). Mọi write qua EF tracking là last-write-wins: MockPayAsync:706-713 (PremiumUntil/HeartsMax), UpdateStreakEagerAsync:933-952, BuyItemAsync:527 (Quantity), EquipItemAsync:620-623 — 2 request song song cùng sửa XP/gems/hearts/premium đều mất cập nhật (lost-update), không có DbUpdateConcurrencyException. | Thêm `[Timestamp] public byte[] RowVersion` (cột rowversion) cho Users + các bảng cộng/trừ (UserInventory, PremiumSubscriptions, UserQuests, UserNodeProgress); migration + bắt DbUpdateConcurrencyException trả 409. Các điểm trừ tiền đã atomic (184/506) vẫn giữ làm lớp chống âm. |
| 4 | GamificationService.cs:494-528 (BuyItemAsync 482-563) | CAO | Shop mua: (a) maxStack check read-then-act (494-502) không atomic — 2 mua song song cùng thấy qty=4 < MaxStack 5 → đều trừ gems, vượt stack hoặc thiếu hàng; (b) `inventory.Quantity += 1` (527) trên entity tracked → lost-update: cả 2 load qty=4, cả 2 ghi 5 → trừ gems 2 lần nhưng chỉ +1 quantity; (c) 2 request mua LẦN ĐẦU (inventory null, 513-523) → cả 2 INSERT → vi phạm UNIQUE(UserId,ItemId) (GamificationConfiguration.cs:74) → DbUpdateException 500 không bắt (khác EnterNode không có catch). | 1 câu lệnh atomic duy nhất: `UPDATE UserInventory SET Quantity = Quantity + 1 WHERE UserId=@u AND ItemId=@i AND Quantity < {MaxStack}` (kiểm tra @@ROWCOUNT ≥1); nếu 0 row → INSERT kèm bắt unique-violation (2601/2627) rồi retry UPDATE; trừ gems trước/trong cùng tx như hiện tại. |
| 5 | GamificationService.cs:273-288 (GetQuestsAsync 257-309) | TRUNG | GET /me/quests check-then-insert không bắt DbUpdateException: 2 request song song đầu ngày (mở app + refresh) đều thấy `existing` rỗng → cả 2 INSERT cùng 5 dòng → vi phạm UNIQUE(UserId,QuestDate,QuestId) (GamificationConfiguration.cs:34) → 500. GET có side-effect ghi mà không idempotent. | Bắt DbUpdateException quanh SaveChanges (insert thất bại = đã tồn tại → bỏ qua) hoặc dùng upsert DB (`ON CONFLICT DO NOTHING` / MERGE); không để GET trả 500 khi chạy lại. |
| 6 | GamificationService.cs:338-345 + 282 (toàn repo: không nơi nào tăng Progress) | CAO | Quest KHÔNG BAO GIỜ hoàn thành được: grep toàn backend không có chỗ nào tăng `UserQuests.Progress` (chỉ INSERT Progress=0 tại 282 và UPDATE Claimed=1 tại 319). ClaimQuestAsync:339 đọc progress=0 < target≥1 → luôn trả VALIDATION_FAILED "Quest chưa hoàn thành". Hệ quả: XP chỉ có nguồn duy nhất từ quest (351) → XP/level/leaderboard đứng yên vĩnh viễn; gems quest cũng chết. | Implement nơi tăng Progress atomic theo ConditionJson (pass node / submit exercise / xem lesson): `UPDATE UserQuests SET Progress = Progress + 1 WHERE UserId=@u AND QuestId=@q AND QuestDate=@today AND Progress < {target}` (+@@ROWCOUNT), trong cùng transaction với hành động học; hoặc nếu thiết kế cố ý để FE tự tăng thì cần API riêng — xác nhận SRS FR-10.3. |
| 7 | GamificationService.cs:690-726 (MockPayAsync 688-736) | TRUNG | MockPay không idempotent, thiếu điều kiện trạng thái: UPDATE không có `WHERE Status = 2` — gọi mock-pay 2 lần trên cùng order → cả 2 đọc order Status=2, đều commit → PremiumUntil gia hạn chồng 2 lần (baseTime = PremiumUntil hiện tại + AddMonths), subscription bị ghi đè. (Gia hạn chồng baseTime:707 là đúng thiết kế cho order mới; sai khi cùng 1 order.) | Kích hoạt bằng UPDATE điều kiện: `UPDATE PremiumSubscriptions SET Status=0, ExpiresAt=@exp WHERE Id=@id AND UserId=@u AND Status=2` → @@ROWCOUNT=0 thì trả "đã active"; đồng thời cập nhật user.PremiumUntil trong cùng tx. |
| 8 | GamificationService.cs:663-676 (UpgradePremiumAsync 653-686) | THAP | orderRef = `DSV{userId}T{months}` (665) trùng cho mọi order cùng plan của 1 user → đối soát CK không phân biệt 2 order; không giới hạn số order pending (spam PremiumSubscriptions). | Thêm hậu tố phân biệt (guid ngắn/sequence) hoặc UNIQUE constraint khi thanh toán; giới hạn pending order/user. |
| 9 | GamificationService.cs:922-957 (UpdateStreakEagerAsync) | THAP | Streak read-modify-write trên entity tracked, không concurrency-safe: 2 EnterNode song song khi LastActivityDate=yesterday → cả 2 đọc StreakFreeze=2 → cả 2 ghi 1 → mất 1 freeze (đáng lẽ còn 0); StreakDays thường đúng (chỉ +1 cố định) nhưng reset/freeze edge sai dưới race. | 1 UPDATE atomic: `SET StreakDays = StreakDays + 1, StreakFreeze = StreakFreeze - 1, LastActivityDate = @today WHERE Id=@u AND LastActivityDate = @yesterday` (và nhánh khác cho reset/freeze) — tự tuần tự hóa theo row lock. |
| 10 | GamificationService.cs:156-159 (EnterNodeAsync) | THAP | catch DbUpdateException bắt MỌI lỗi (FK, constraint khác, timeout) → coi là "session đã tồn tại" → resume + trả 200; che dấu lỗi thật, không log. Nhánh INSERT/UNIQUE(UserId,NodeId) (LearningPathConfiguration.cs:68) về cơ bản ổn để chống double-spend, chỉ cần thu hẹp catch. | Chỉ resume khi là unique violation (SqlException 2601/2627 trên IX_NodeSessions_UserId_NodeId), các lỗi khác → rollback + 500/log. |
| 11 | GamificationService.cs:822 (ComputeHearts 814-835), 866 | THAP | Không clamp dưới 0: `Math.Min(max, user.Hearts + regenCount)` — nếu DB Hearts bị âm (lỗi khác) UI hiện âm. Defense-in-depth. | `Math.Max(0, ...)` tại ComputeHearts + PersistHeartRegenAsync; các UPDATE trừ đã có `Hearts > 0` (184) nên đây chỉ là lưới an toàn. |
| 12 | GamificationService.cs:842 (HeartConfig) + 634-650 (GetPremiumStatusAsync) | THAP | Quyền lợi premium chỉ có 1 nơi tính (HeartConfig) — benchmark/cheatsheet/hint KHÔNG bị gate premium (không check PremiumUntil ở đâu khác; nếu SRS yêu cầu premium-only thì thiếu implement). Ngoài ra 2 nguồn sự thật: status API đọc PremiumSubscriptions.ExpiresAt, quyền lợi đọc user.PremiumUntil → có thể lệch (VD subscription Status=1 nhưng PremiumUntil còn hạn → status "none" nhưng vẫn được 30 tim). | Thống nhất 1 nguồn (user.PremiumUntil) cho cả status lẫn quyền lợi (hoặc đồng bộ khi cancel); xác nhận với SRS danh sách tính năng premium-only và gate tương ứng. |
| 13 | GamificationService.cs:405-413, 449 (GetLeaderboardAsync 396-454) | THAP | Leaderboard read-only → không race; nhưng tab "week" lọc theo LastActivityDate ≥ weekStart (đúng FR-10.6) trong khi xếp hạng theo TỔNG Xp (Value = Xp, đã comment G-F3E-NEW-1) → "week" không phản ánh XP trong tuần, chỉ là bộ lọc user. Không phải lỗi race nhưng cần confirm thiết kế với SRS. | Nếu FR-10.6 yêu cầu XP tuần: thêm bảng/ cột XP tích lũy theo tuần (cập nhật atomic cùng chỗ cộng XP); ngược lại giữ nguyên và ghi rõ trong API_REFERENCE. |

## Tổng kết
- Tổng số findings: **13** (1 BUG-1 đã biết + 12 mới)
- Mức: **CAO: 5** (BUG-1, #2, #3, #4, #6) · **TRUNG: 2** (#5, #7) · **THAP: 6** (#8, #9, #10, #11, #12, #13)

## Ghi chú điểm đã kiểm tra và ĐẠT (không phải finding)
- Trừ tim EnterNode (184): `UPDATE ... Hearts > 0` atomic + UNIQUE(UserId,NodeId) (LearningPathConfiguration.cs:68) + nhánh INSERT bắt exception → chống double-spend tim đúng thiết kế (chỉ cần thu hẹp catch, #10).
- Trừ gems mua shop (506): `UPDATE ... Gems >= price` atomic — gems không thể âm; vấn đề nằm ở inventory (#4).
- Claim quest (319): UPDATE điều kiện `Claimed=0 AND QuestDate=today` + @@ROWCOUNT → idempotent/atomic đúng; unique (UserId,QuestDate,QuestId) tồn tại (GamificationConfiguration.cs:34); lỗi duy nhất là Progress không bao giờ tăng (#6).
- Cộng XP/gems quest (351) nằm cùng transaction với Claimed=1 → nhất quán.
- XP submit exercise: ExerciseService.SubmitAsync KHÔNG cộng XP (chỉ ExerciseSubmission/UserProgress/UserNodeProgress) — XP chỉ từ quest (xem #6).
- Không có hosted service/job nào → BUG-1 không có job downgrade (xác nhận).
