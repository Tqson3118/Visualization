# FIX-LOG — Nhóm C (Profile/Shop/Quests/Leaderboard/Premium/Subscription)

> Format: `view | hygiene trước | hygiene sau | đặc-trưng trước | đặc-trưng sau | thay đổi chính`. Ngày: 13–14/08/2026.

| view | hygiene trước | hygiene sau | đặc-trưng trước | đặc-trưng sau | thay đổi chính |
|---|---|---|---|---|---|
| ProfileView `/profile` | 72/100 | 91/100 | 2/10 | 8/10 | Hero: bỏ gradient/blob/shadow → surface band level-2; stat: 5 card đồng nổi 800 → 1 hero XP (BlockToken canvas-ink + index mono) + stat phụ level-1 + streak block-token resolved; bỏ emoji (🔥💎❤️🏆🛒👥🏅🔒) → lucide; radar → nền tối canvas-ink + màu data-core; quick-link index mono 01–04; error state + retry (EmptyState chung); bỏ hover-lift/shadow card; weight 800/700 → 600; nút submit md; hero-stat enter 300ms chuẩn easing. |
| LeaderboardView `/leaderboard` | 73.5/100 | 91/100 | 3/10 | 8/10 | Rank: medal emoji 🥇🥈🥉 → block-token chip tối canvas-ink (top1 warning) + header mono RANK/USER/VALUE; bỏ gradient hero/blob/title + gradient overlay top-3 rows + me-badge gradient → border-color/tint + bg-primary; chart top-10 → vùng dữ liệu luôn tối (màu data-core/index-muted/warning); EmptyState error + retry, noClass + CTA "Đi tới Lớp học"; FLIP 450ms ease → 300/250/200ms chuẩn easing; value mono; bỏ shadow pinned; H1 48px. |
| ShopView `/shop` | 76/100 | 91.5/100 | 3/10 | 7.5/10 | Hero: bỏ gradient/blob/shadow → surface band; gems = 1 hero-stat BlockToken (tối + index mono) + item count stat phụ; bỏ tint gradient icon slot (aurora/mint/sunset) → chip muted đồng nhất; price amber → mono + foreground; nút "Mua" sm → md (40px); card hover → border-color; bỏ shadow card. |
| QuestsView `/quests` | 77/100 | 91.5/100 | 3/10 | 7.5/10 | Hero: bỏ gradient/blob/shadow → surface band + strip DONE mono; streak chip gradient-sunset + shadow → BlockToken resolved (nền tối); card--ready gradient → border+tint; reward/XP amber → mono + token; nút claim sm → md; confetti khi claim quest cuối (disableForReducedMotion); toast bỏ 💎 (cùng động từ "Đã nhận"); weight 800/700 → 600; H1 48px. |
| PremiumView `/premium` | 68/100 | 93.5/100 | 2/10 | 8/10 | Hero gradient aurora + blob + title gradient-clip + shadow → surface band level-2 + strip mono `PLAN · 1M · 3M · 12M` (dữ liệu tuần tự, canvas-ink); highlight plan gradient+shadow-lg+scale → border+tint success (pattern quests__card--ready); bỏ shadow mọi card (`.card` override); bảng so sánh: emoji ✔✘❤ → lucide Check/X (success/quaternary), th hết uppercase/tracking dương → §4.6, mobile → card-stack (thead ẩn + data-label); giá/countdown/account/amount/CK → mono; success ✔ emoji → BlockToken resolved (PREMIUM + tên gói) + enter 300ms chuẩn (khoảnh khắc đầu tư duy nhất) + confetti; bỏ emoji 🎉 i18n; price/cột premium/countdown bỏ accent primary trang trí; H1 48px; weight 800/700 → 600; clear redirectTimer khi unmount; nút copy sm → md; QR frame trắng giữ (functional — decision log). |
| SubscriptionView `/account/subscription` | 72/100 | 93.5/100 | 2/10 | 8/10 | Hero gradient + blob + shadow + title gradient → surface band level-2; days-left chip primary tint + padding 14px → hero-stat BlockToken resolved "CÒN LẠI n ngày" (1 hero-stat/màn, LUÔN tối) + enter 300ms chuẩn (khoảnh khắc đầu tư); error state MỚI (gọi API trực tiếp — store nuốt lỗi) + EmptyState retry "Thử lại"; bỏ emoji ❤ (benefits/loses → i18n); ngày hết hạn → mono tabular; weight 700/800 → 600; H1 48px; gap 6px/14px lẻ → token; bỏ shadow card. |

## Ghi chú chung
- **Fix app-wide** (xem `docs/pm-decision-log-viewquality.md`): XÓA block reset `*{margin/padding:0}` khỏi global.css (unlayered đè mọi padding utility Tailwind → button shadcn padding 0px; Tailwind preflight trong `@layer base` thay thế — verify sau fix: button padding 12px); Tabs.vue `min-h-9` (tab 22px → 38px).
- Component chung mới: `frontend/src/components/ui/BlockToken.vue`.
- Ngoại lệ duy nhất "0 hex rời" (decision log 14/08): QR frame `bg-white` — chuẩn QR ISO/IEC 18004 yêu cầu nền sáng để scan (qrcode lib vẽ module đen trên nền trong suốt).
- Verify thực tế (Playwright + route-mock /api/v1 vì BE :5000 trả 404 cho route thật): 2 view light+dark+390px — overflowX = 0, console error = 0 (trừ 1 lỗi 500 mock có chủ đích khi test error state), button ≥40px, block-token nền `rgb(13,16,32)` = canvas-ink ở mọi theme, hero `bgImage: none`, card `box-shadow: none`, H1 48px/-0.03em, table mobile thead hidden + ::before data-label "Free/Premium", success flow: QR 208px → countdown → confirm → BlockToken resolved enter 300ms `cubic-bezier(0.16,1,0.3,1)` → redirect home.
- Cảnh báo console duy nhất còn lại (component chung, có ở mọi Modal): `[WARNING] Missing Description or aria-describedby for DialogContent` — shadcn-vue Dialog nội bộ; đề xuất Phase 2 thêm `aria-describedby`/description vào Modal.vue.
- Nút md 40px giữ nguyên trên mobile (đồng nhất nhóm C đã chốt — §8 khuyến nghị 44px cho CTA chính; ghi notes, Phase 2 cân nhắc Button auto-upgrade lg theo breakpoint).
- Build: `npm run build` PASS (vue-tsc + vite). Unit test: `npm run test` — 12 files / 95 tests PASS.
- Ollama 3 gate × 6 view (qwen2.5vl:3b): không lỗi spacing (gate2); bản sắc nhận diện "DSA Visual / data structure learning" + block tối (gate3) — chi tiết `docs/work/view-quality/ollama-log/<view>.md`. Gate1 2 view mới: Premium 7.5/10, Subscription 8.5/10 (nhận xét "card shadow"/"font lớn hơn dark" là hallucination model 3b — computed style khẳng định shadow none, font giống hệt).
