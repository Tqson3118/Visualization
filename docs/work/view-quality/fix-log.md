# FIX LOG — Phase 1 nhóm A (9 view landing/auth + 404)

> Bảng trước/sau theo `standard.md` (10 trục hygiene + Đặc trưng tách riêng). Audit chi tiết từng view tại `audit-<view>.md`. Ngày: 13/08/2026.

| view | hygiene trước | hygiene sau | đặc-trưng trước | đặc-trưng sau | thay đổi chính |
|---|---|---|---|---|---|
| HomeView `/` | 64.5 | 95.0 | 2 | 9 | Bỏ header nội bộ (fix 2 header chồng — AppHeader toàn cục giữ); hero: bỏ gradient aurora + blob + shadow → surface band level-2 + panel demo tối canvas-ink chạy **mini-sim step THẬT từ engine** (sort.bubble / search.binary / graph.bfs qua `getSimulation()` + `generate()`, DOM block + index mono, status data-core/resolved/conflict, counter `bước x/y`, tabs aria-pressed, prefers-reduced-motion dừng autoplay); demo-btn `<button>` raw → Button.vue; icon squares gradient → muted surface; stat-card bỏ gradient/800/shadow → level-1 Geist 600; CTA RouterLink qua `buttonVariants`; font-weight ≤600; tracking âm heading; hover card chỉ đổi border |
| LoginView `/login` | 65.5 | 95.0 | 2 | 8 | Aside gradient aurora + blob + glassmorphism → panel tối `canvas-ink` + badge mono + block-token strip + index mono; title hết gradient (H1 48px/600/-0.03em); shell bỏ `shadow-xl` (elevation = surface + border); icon BaseIcon → lucide-vue-next (Input.vue hỗ trợ icon Component); nút submit size lg (44px); forgot/back link hit-target ≥ 24px |
| RegisterView `/register` | 61.0 | 95.0 | 2 | 8 | Như LoginView + segmented vai trò 2 `<button>` raw → Button.vue ghost + `aria-pressed` + active bg-card/border (bỏ shadow-sm); ✓/○ glyph → lucide Check/Circle; Motion easing `easeOut 0.32s` → `[0.16,1,0.3,1] 0.28s`; teacher box bỏ tô primary 4% → `bg-muted`; textarea `.input` legacy → token class; checkbox hit 18px; giữ nguyên selector e2e (form.register__card / label.register__row / button.register__role-option) |
| ForgotPasswordView `/forgot-password` | 66.0 | 95.0 | 2 | 8 | Aside tối Data Bench (như trên); bỏ `←` ký tự → ArrowLeft lucide; sent-icon tròn → square radius-md (hết "icon tròn"); Motion easing chuẩn; shell hết shadow; weight ≤600 |
| ResetPasswordView `/reset-password` | 64.0 | 95.0 | 2 | 8 | Như Forgot + timer redirect `setTimeout` có `onUnmounted` cleanup (trục 9); checklist ✓/○ → lucide; v-for key ổn định (key rule.key thay idx) |
| NotFoundView `404` | 55.5 | 96.5 | 1 | 9 | Bỏ vòng gradient aurora + Compass + shadow-xl + 800 + heading gradient → panel tối canvas-ink "mảng mất index": block 4·0·4 (data-core) + block 03 dashed conflict "out of bounds" + index mono 00-03 + label mono; CTA qua buttonVariants lg; Motion 280ms chuẩn |
| PrivacyView `/privacy` | 54.0 | 94.0 | 1 | 7 | Hero aurora-soft + gradient icon/title + shadow → surface band level-2; TOC raw `<button>` → native anchor `#sec-N` (Lenis smooth, bỏ JS); index mono 01-06 TOC + section; H1 48px; toc-link 8/12px; bỏ shadow TOC; h2 section H4 24px/-0.015em |
| HelpView `/help` | 54.0 | 94.5 | 1 | 7 | Hero aurora-soft + gradient → surface band level-2; FAQ `.card` legacy (shadow + all 250ms ease) → token card; trigger raw `<button>` → buttonVariants ghost + aria-controls; chevron/FAQ transition easing chuẩn; submit lg 44px; FAQ index mono 01-06; sent-title 700→600 |
| SimulationsView `/simulations` | 53.0 | 94.0 | 3 | 9 | Chrome gradient mint + shadow → surface band level-2; strip block-token + index mono trong banner; stat bỏ gradient/800 → Geist 600 + label tertiary; card bỏ hover-lift → hover đổi border + Space key; BenchmarkPanel/CheatSheetTable → defineAsyncComponent (lazy tab); badge min-h-6 (Badge.vue shared) |
| CheatSheetView `/cheatsheet` | 55.0 | 94.0 | 3 | 9 | Chrome gradient mint ×3 (bg/icon/title) + shadow + overlay → surface band level-2 + strip Big-O block-token `BIG-O 00–04` (chip tối canvas-ink + index mono); H1 48px/600/-0.03em; badge count primary→muted; raw `<button>` ×2 (chip lọc + "▶ Xem mô phỏng") → Button shadcn outline sm (aria-pressed; Play lucide); Big-O text mono trần → block-token chip tối canvas-ink + mono text-sm; th hết uppercase/tracking dương + scope=col; bảng mobile ≤640 → card-stack (cấm scroll ngang §8, td data-label); hardcode chuỗi → i18n cheatsheet.*; search input thêm name (a11y issue 0) |

## Ghi chú chung
- **HOTFIX toàn app (đã ghi decision log)**: `global.css` reset `* { padding: 0 }` unlayered đè mọi padding utility Tailwind (đo thật: input có `px-3 pl-9` nhưng computed `paddingLeft=0px`) → đưa vào `@layer base`; khôi phục padding thiết kế cho mọi shadcn control (không chỉ 5 view).
- **HOTFIX 2 toàn app (đã ghi decision log)**: `global.css` `a { color: var(--color-primary) }` unlayered đè `text-primary-foreground` trên mọi RouterLink-as-button → chữ teal trên nền teal ~1.2:1 (fail WCAG, đo thực tế CTA HomeView + 404) → đưa vào `@layer base` + `a.inline-flex:hover { text-decoration: none }`; sau sửa CTA 404 = trắng/teal, contrast 21:1.
- **Page transition easing**: `220ms ease` → enter `cubic-bezier(0.16,1,0.3,1)` / exit `cubic-bezier(0.7,0,0.84,0)` (global.css, ảnh hưởng mọi route).
- **Input.vue** (dùng chung 10 view): thêm prop `icon` nhận `string | Component` — string legacy vẫn qua BaseIcon, Component mới (lucide) — backward-compatible.
- **Badge.vue** (shared, decision log 2026-08-13): thêm `min-h-6` — badge height ≥ 24px (trục 5f); đo thực tế 25.6px.
- Bằng chứng re-audit: đo computed style (padding button 32/12px, input 12px+icon, height 44/40/36px), 3 mốc breakpoint (1366/768/390 — không overflow, không đè chữ), console error = 0, 95/95 test PASS, build PASS.
