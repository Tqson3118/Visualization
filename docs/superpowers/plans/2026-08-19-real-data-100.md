# Real Data 100% and 23 Screenshots Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development or superpowers:executing-plans.

**Goal:** Complete the real-data backend, deterministic seed coverage, 23 browser screenshots, and synchronized documentation required by PROMPT_REAL_DATA_100_SCREENSHOTS_v5.md.

**Architecture:** Preserve existing backend/frontend contracts. Complete missing backend routes and DTOs first, seed relational data deterministically, exercise the real browser flow against :5055/:5174, then re-embed verified screenshots into the DOCX. Existing working-tree changes remain intact; only explicitly scoped paths are staged.

**Tech Stack:** ASP.NET Core/.NET 10, EF Core SQL Server, Vue/Vite, Vitest, Playwright, python-docx, SQL Server 2022.

## Global Constraints
- Do not reset or overwrite unrelated staged/untracked work.
- Do not use git add -A.
- Keep data endpoints real; only /api/v1/auth/refresh may be intercepted for screenshot session persistence.
- Preserve frontend/tests/e2e/helpers/mockApi.ts response shapes.
- Do not add notification/forum/certificate screenshot scope.
- Required scale: 20 courses, 120 students, 8 teachers, 12 classrooms, 25 simulator keys, 14 badges.

## Phase A — Backend/API checkpoint
### Task 1: Establish baseline and probe artifacts
- Create future/real-data-probe-before.md; run existing backend build and endpoint probe with student/admin tokens; classify every 404/409/5xx.
### Task 2: Repair runtime infrastructure
- Correct SQL Server healthcheck password via environment substitution; ensure vite.real.config.ts has Vue/Tailwind, @ alias, port 5174 and proxy :5055; no literal SA password in production config.
### Task 3: Implement profile/gamification contracts
- Implement and verify /progress/me, /me/gamification, /me/hearts, /me/streak, /me/quests, /me/inventory, /achievements, /premium/status with FE-compatible shapes.
### Task 4: Implement simulations and remaining API aliases
- Implement /simulations plus detail/schema/demo for all 25 keys and every route identified by the baseline probe.
### Task 5: Add required schema changes and clean migration
- Add only §1b fields/entities; generate a clean EF migration, remove failed migrations immediately, and build after model changes.
### Checkpoint A
- Save future/real-data-probe-after.md; require zero unexpected 404/409/5xx and backend build zero errors; commit explicit backend/config paths.

## Phase B — Deterministic seed checkpoint
### Task 6: Seed content scale and simulator catalog
- Seed 20 courses, modules, lessons, quizzes, module items, theory articles, learning paths/nodes and 25 simulator keys.
### Task 7: Seed roster, classrooms, progress and badges
- Seed 8 teachers, 120 students, deterministic identities, 12 classrooms with 10 students each, progress/XP/streak/hearts, 14 badges and earned badges.
### Task 8: Seed quests, shop, inventory and gem ledger
- Seed daily/weekly/monthly quests, user states, shop items, equipped inventory and balanced gem transactions.
### Task 9: Seed orders, reviews and feedback
- Seed 25–40 orders, premium/gem purchases, valid payment codes, completed-student course reviews, lesson comments/replies and lesson reviews.
### Task 10: Seed codelabs and exercise coverage
- Seed 6–8 codelabs with code/testcases/hints/four-language templates and quizzes for courses 11–20; use shape-only QC if Piston is unavailable.
### Checkpoint B
- Drop/recreate/seed twice; compare counts and roster/order hashes; run §2.5 SQL checks; save future/real-data-consistency.md; update DBML and database guide; commit explicit paths.

## Phase C — Screenshots and browser QC
### Task 11: Start services and validate flows
- Run backend :5055 and frontend :5174 with one worker; use Playwright 1440×900 dark theme; validate public/student/admin flows and console/network.
### Task 12: Capture and QC 23 screenshots
- Create/update the 23 required files under tailieu/screenshots; verify DOM data, no error/empty/skeleton, no console 404; optional Ollama, otherwise record QC fallback: text.

## Phase D — DOCX and final gates
### Task 13: Synchronize docs and embed screenshots
- Update API reference, DBML, database guide and required DOCX sections; run only python word_format_run.py --steps=M; preserve captions and skip Hình 4.10.
### Task 14: Run final gates and report
- Run frontend build, Vitest, Playwright, backend build, PNG verification, DOCX verification and git diff --check; save final report and commit explicit paths.

## Risks and Mitigations
- Existing working-tree changes: inspect status before every commit and stage explicit paths.
- Prompt count contradiction (128 vs 137): report actual deterministic seed count and pending-user treatment.
- SQL Server/Piston unavailable: separate blocker from shape-only QC and never fabricate results.
- Existing backend tests broken: scope verification to build/probe and record unrelated failures.
- DOCX automation risk: backup DOCX and verify image/caption counts after --steps=M.