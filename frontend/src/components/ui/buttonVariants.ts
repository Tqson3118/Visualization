// buttonVariants — API công khai của kit cho phần tử KHÔNG phải <button>
// (RouterLink, <a>, ... cần class kiểu nút).
// App code chỉ được import qua module này, KHÔNG import trực tiếp từ './button'
// (thư mục kebab-case là nội bộ kit — xem README.md cùng thư mục).
export { buttonVariants } from './button';
