namespace DsaVisual.Application.Services;

/// <summary>
/// Danh mục mô phỏng + schema (đồng bộ khóa frontend catalog — SDD §6.1, §9.9).
/// TODO: triển khai — skeleton v2 theo SDD §5.1.
/// </summary>
public interface ISimulationCatalogService
{
}

public sealed class SimulationCatalogService : ISimulationCatalogService
{
    // TODO: implement (SDD §5.4, §6.1, API_REFERENCE §4.5)
}
