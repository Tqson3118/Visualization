# HANDOFF — 12/08/2026 (phiên tiếp theo bắt đầu từ đây)

> File này để phiên sau tiếp tục công việc. Đọc kỹ trước khi làm. Mọi chi tiết nằm trong docs/ — KHÔNG đoán.

## 1. DỰ ÁN
- Đồ án tốt nghiệp FPT (GVHD **Phạm Ngọc Ái Liên**, Lớp **SD21361**, 4 thành viên): **Hệ thống hỗ trợ học tập và trực quan hóa CTDL & giải thuật (DSA-Visual)**.
- Nhóm: Mai Tiểu Bảo TD01287 (Trưởng nhóm, backend) · Thái Quang Sơn TD01282 (frontend) · Huỳnh Lê Minh Thư TD01131 (engine + test) · Trần Viết Tâm Phúc TD01261 (tài liệu + code hỗ trợ).
- Mốc thời gian chính thức: khởi động 12/05/2026, phát triển hết 11/08/2026, báo cáo/bảo vệ cuối tháng 8. **CẤM ghi "20 tuần/16 tuần"** trong báo cáo — chỉ 13 tuần.

## 2. TRẠNG THÁI (đã kiểm chứng 12/08 — REVIEW PASS 3/3)
- ✅ **Docs 12/12 file** đạt chuẩn PRODUCTION_PROMPT §17.2 + checklist §17.9 (xem `docs/pm-report.md`): SRS 1296 dòng, SDD 2042 dòng, API 730, TEST_PLAN 759, USER_GUIDE 503, DEPLOY 369, GLOSSARY 105, SCREEN_MAP 306, catalog JSON 44 entries, THIRD_PARTY, READMEs.
- ✅ **Session A — Báo cáo Word DONE**: `tailieu/BAO_CAO.md` (149KB, khuôn trường PHẦN 1-7) + `tailieu/BaoCaoDoAn.docx` (298KB, pandoc --toc, 18 ảnh nhúng) + `tailieu/placeholders/` (18 PNG 1920×1080 có nhãn) + `tailieu/diagram-prompts.md` (6 prompt ChatGPT). Log: `docs/pm-report-a.md`, `docs/pm-decision-log-a.md`.
- ✅ **Session B — Bê code DONE**: `frontend/` (84 file, Vue3+Vite+TS) + `backend/` (87 file, DsaVisual.Api + DsaVisual.Application 2 project theo SDD §11.1) + docker-compose (SQL Server + MailHog). Bê 24 mục từ 3 source (chi tiết `docs/REUSE_REPORT.md` — mỗi dòng có cột "Khớp mục SDD"). Verify đã chạy: frontend build PASS + 39/39 test, backend build 0 warning + 8/8 test, smoke /health OK. Grep cấm sạch (không PostgreSQL/Repository/MediatR/secret). Log: `docs/pm-report-b.md`, `docs/pm-decision-log-b.md`.
- ✅ **Session C — Git DONE**: repo tại root, nhánh `main` + `dev`, push xong lên https://github.com/Tqson3118/Visualization (9 commit, 4 author = 4 email thật). Log: `docs/pm-report-c.md`, `docs/pm-decision-log-c.md`.
- ✅ **3 source vật liệu** trong `source/`: VisualizationDSA1 (bản tốt nhất), VisualizationDSA3 (curriculum), VisualizationDSA (gốc).

## 3. VIỆC CÒN LẠI (ưu tiên theo thứ tự)
1. **Commit 14 file untracked + push** (output của A/B sinh sau commit của C — `git status --short` thấy: `backend/`, `frontend/`, `docker-compose.yml`, `docs/REUSE_REPORT.md`, `docs/pm-report-a/b.md`, `docs/pm-decision-log-a/b.md`, `tailieu/BAO_CAO.md`, `BaoCaoDoAn.docx`, `diagram-prompts.md`, `parts/`, `placeholders/`). Dùng `.\commit-as.ps1`: docs/tailieu → `phuc`, backend → `bao`, frontend → `son`, engine/REUSE → `thu`. **User CHƯA xác nhận — hỏi trước khi commit.**
2. **Xóa `simulator-empty.png`** (file lạ ở root — dư từ test playwright).
3. **Xác nhận email GitHub thật của Thu/Phúc** trong `commit-as.ps1` (đang đoán pattern username@gmail.com) → ghi vào `docs/SETUP_TODO.md` mục 0.
4. **Thay 18 placeholder ảnh bằng ảnh thật**: 6 sơ đồ dùng ChatGPT (prompt sẵn trong `tailieu/diagram-prompts.md`) → ảnh về đặt `tailieu/diagrams/`; 12 màn UI chụp Playwright sau khi app chạy → `tailieu/screenshots/` → chạy lại: `pandoc tailieu/BAO_CAO.md -o tailieu/BaoCaoDoAn.docx --toc`.
5. **Điền số liệu thật**: kết quả kiểm thử (đang ghi "chờ tuần 19-20"), THIRD_PARTY bổ sung license đầy đủ, ngày bảo vệ trên bìa (để trống).
6. **Code tiếp theo docs**: skeleton đã có nhưng còn stub (27 view, 14 controller, generator stub, chưa có Migrations — xem mục LỆCH trong `docs/pm-report-b.md` + `docs/REUSE_REPORT.md`); seed plan mới chưa viết. Sau này chạy feature code theo quy trình git: `feature/<tên-task-to>` → PM review → PR vào `dev`.

## 4. QUY TRÌNH LÀM VIỆC (bắt buộc tuân theo)
- PM workflow: `/pm "..." --auto` — làm rõ bằng giả định (ghi `docs/pm-decision-log-<tên>.md`), KHÔNG checkpoint, cuối mỗi task ghi `docs/pm-report-<tên>.md`. Chạy song song 2-3 session, log riêng từng session (tránh ghi đè).
- Nguồn chuẩn mọi việc: `docs/PRODUCTION_PROMPT.md` (v2.12, 448KB) > `docs/PM_MASTER_PLAN.md` (kế hoạch 3 session) > `docs/BAO_CAO_SPEC.md` (báo cáo) > `docs/DIAGRAM_PROMPTS.md` (ảnh sơ đồ).
- Git: `main` (ổn định) + `dev` (tích hợp) + `feature/x`; commit message Conventional Commits; commit theo tên thành viên qua `commit-as.ps1`; KHÔNG commit .env/secret/node_modules (đã có .gitignore chuẩn).
- Khi cần user: gom vào `docs/SETUP_TODO.md` (đã có mục 0-3 sẵn).
- Verify trước khi báo xong: lint/typecheck/test/build; repo không có lệnh nào thì ghi rõ, không đoán.

## 5. MÔI TRƯỜNG / CÔNG CỤ (đã cài)
- **pandoc**: `C:\Users\Administrator\AppData\Local\Pandoc\pandoc.exe` (build docx).
- **Ollama vision**: `qwen2.5vl:3b` chạy local (API http://localhost:11434) — mô tả ảnh khi cần; model hiện tại KHÔNG đọc được ảnh → kiểm tra UI bằng DOM assertions + console (chrome-devtools/playwright MCP) thay vì nhìn ảnh.
- **MCP**: context7, chrome-devtools, playwright, memory, duckduckgo (mới cài — cần restart opencode để load), github (cần GITHUB_TOKEN).
- **Skill**: 200+ cài sẵn; dev-frontend BẮT BUỘC nạp frontend-design + verify UI bằng browser (đã sửa `.opencode/agent/dev-frontend.md`).
- **Agent**: pm (edit: deny — chỉ điều phối), dev/dev-backend/dev-frontend (được sửa file — đã test thật).

## 6. PROMPT MẪU CHO PHIÊN TIẾP (dùng khi user yêu cầu tiếp tục)
```
/pm "Đọc session/HANDOFF_2026-08-12.md + docs/PM_MASTER_PLAN.md + docs/SETUP_TODO.md.
Xử lý việc còn lại theo thứ tự: (1) commit 14 file untracked theo phân công + push,
(2) xóa simulator-empty.png, (3) cập nhật mọi việc cần người dùng vào SETUP_TODO,
(4) nếu ảnh thật có sẵn → thay placeholder + build lại docx bằng pandoc.
Ghi log vào docs/pm-decision-log-a.md, trạng thái vào docs/pm-report-a.md." --auto
```
