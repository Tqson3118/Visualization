# HANDOFF — 13/08/2026 (quản lý tổng — phiên tiếp theo bắt đầu từ đây)

> File này tóm tắt TOÀN BỘ trạng thái dự án + các session đang chạy + prompt sẵn sàng. Phiên mới đọc file này là nắm hết, KHÔNG cần đọc lại lịch sử cũ.

## 1. DỰ ÁN
- Đồ án tốt nghiệp FPT (GVHD Phạm Ngọc Ái Liên, Lớp SD21361, 4 thành viên): **DSA-Visual** — hệ thống hỗ trợ học tập + trực quan hóa CTDL & giải thuật.
- Nhóm: Mai Tiểu Bảo TD01287 (backend) · Thái Quang Sơn TD01282 (frontend) · Huỳnh Lê Minh Thư TD01131 (engine+test) · Trần Viết Tâm Phúc TD01261 (tài liệu).
- Mốc: khởi động 12/05/2026, phát triển hết 11/08/2026, bảo vệ cuối tháng 8. **CẤM ghi "20 tuần/16 tuần" — chỉ 13 tuần.**

## 2. TRẠNG THÁI TỔNG (kiểm chứng 13/08)
- ✅ Docs 12/12 + báo cáo Word + git: `main` (ổn định, chưa merge dev→main — PR #1 mở chờ) + `dev` (tích hợp đầy đủ).
- ✅ Đợt D: backend services/controllers/DTO/migration 32 bảng + seed thật + 44 generator + 33 view. FE 72/72 test, BE 44 unit + 27 integration, e2e 11/11.
- ✅ Đợt E: runMeasure Web Worker + renderer canvas thật + Playwright e2e + integration tests (Testcontainers).
- ✅ Đợt F: THIRD_PARTY thật + checklist §17.9 + TEST_PLAN số liệu.
- ✅ Đợt G: stack mới **tailwindcss 4 + shadcn-vue + motion-v + GSAP + vue-echarts + Lenis + vue-sonner + lucide/phosphor + font Geist/JetBrains Mono** đã merge dev. G-F3D: 12 màn chính 11 PASS/1 lỗi (đã fix G-F3E/G-F3E2). Bug canvas ResizeObserver đã fix.
- ✅ Đợt G-PHU: **9/9 DONE + APPROVE** (feedback endpoint, 2FA MailHog, lesson-sim-keys, breakpoints, **premium QR MB Bank** NGUYEN THI NHU HOA 83863112088386 — nội dung CK tự động DSV<UserId>T<months>, kích hoạt tự động sau 60s, docs đã đồng bộ SRS/SDD/USER_GUIDE/API_REFERENCE/THIRD_PARTY, NFR-5 đo lại 852KB gốc không nới).
- ✅ Git: 8 PR ma base main đã đóng (còn PR #1 dev→main). Quy tắc: PR base `dev`.
- ✅ MCP: exa (key 6e92b042... — đã verify), fetch, context7, playwright, chrome-devtools, memory, github (token fine-grained), duckduckgo. **notebooklm MCP ĐÃ TẮT** (auth fail nhiều lần — profile bị lock bởi node process cũ; đã cleanup_data + kill node/chrome, chưa login thành công; user quyết bỏ dùng).
- ✅ Agent (đội hình 10): pm · dev · dev-backend · dev-frontend · dev-engine · dev-ux · dev-test · dev-e2e (Playwright + Ollama qwen2.5vl:3b — model chính KHÔNG đọc ảnh) · dev-review · dev-docs. Skill `pm-prompt-std` (khuôn prompt 7 phần). MCP github cần GITHUB_TOKEN đã set.
- ✅ Review chéo 2 prompt I/J (user tự viết) đã nhúng khối bài học + BUG-1.

## 3. CÁC SESSION ĐANG CHẠY (song song — KHÔNG đụng nhau)
| Session | Prompt | Nơi chạy | Nhánh | Trạng thái |
|---|---|---|---|---|
| **H — UI review 48 màn** | `PROMPT_H_UI_REVIEW.md` | `D:\FPT\neww` | feature/ux-h-* → dev | ✅ **DONE 13/08 — đã merge dev**: b/c/d/e3/final1/docs + **e1 (2 commits cuối, merge c20ab16)** |
| **Diagrams — 6 ảnh drawio** | `PROMPT_DIAGRAMS_DRAWIO.md` | **`D:\FPT\neww-diagrams`** (worktree) | feature/diagrams — commit được, KHÔNG merge dev | ĐANG CHẠY ở worktree riêng |
| **J — Backend audit** | `PROMPT_J_BACKEND_AUDIT.md` | `D:\FPT\neww` (LOCAL) | feature/backend-audit → **đã merge dev (3e08be2)** | ✅ **DONE 13/08**: 93 findings (14 CAO fix 100%), 137 unit + 77 integration PASS, review APPROVE |
| **K — Seed production-like** | `PROMPT_K_SEED_PROD.md` | `D:\FPT\neww-seed` | feature/seed-prod | ✅ **DONE — PR #10 merged dev 13/08** |
| **L — Form đăng ký giảng viên** | `PROMPT_L_TEACHER_REGISTER.md` | worktree `D:\FPT\neww-teacher` | feature/teacher-register → **đã merge dev (90cb339, resolve 3 conflict: User.cs/snapshot/RegisterView)** | ✅ **DONE — PR #11 merged thủ công 13/08** |
| **M — Final review toàn bộ** | `PROMPT_M_FINAL_REVIEW.md` | worktree `D:\FPT\neww-review` | feature/final-review-2 → PR base dev | CHƯA CHẠY — chạy SAU khi I/II xong (review cuối trước bảo vệ) |
| **N — Tối ưu theo backlog** | `PROMPT_N_OPTIMIZATION.md` | worktree `D:\FPT\neww-opt` | feature/optimization → PR base dev | CHƯA CHẠY — chạy SAU M |

**13/08 tối — merge batch (user yêu cầu)**: J (3e08be2) → canvas vis-layout (eecc0a5, bar mode V3 — arrayRenderer + canvasPainter) → H-E1 (c20ab16) → L (90cb339, conflict User.cs RowVersion+3 cột GV / snapshot / RegisterView nền H-E1 + form GV L, 2 fix: markAllTouched + pending text auth.teacherPendingSuccess) → docs batch (c1d1203). Verify sau merge: BE build 0/0 + **137 unit + 77 integration PASS** (thêm 16 test L), FE vue-tsc sạch + **95/95 PASS** (RegisterView.spec 6/6). **Migrations đã apply DB thật** (ef database update: AddRowVersionConcurrency + AddSubmissionUniqueConstraints + RemovePermanentSubmissionUnique + AddLeaderboardIndexes + AddContentUniqueIndexes + AddTeacherProfileFields — Users có đủ RowVersion/Department/StaffCode/TeacherBio). **Dev pushed origin/dev (c1d1203)**. Đã merge xong H/J/K/L → còn: I/II → M → N → PR #1.

⚠ **Lưu ý H vs Diagrams**: user đã CHỦ ĐỘNG XÓA 6 ảnh SVG cũ tailieu/diagrams/* (lỗi + xấu) — vẽ mới từ đầu. Diagrams chạy ở worktree riêng nên không đụng working tree chính.

## 4. PROMPT SẴN SÀNG (session/ — đã research-verified + nhúng bài học)
- **`PROMPT_H_UI_REVIEW.md`** — đợt H: review + nâng cấp 48 màn (38 route), 5 nhóm, checklist 10 tiêu chí, vòng Ollama 7 tiêu chí (≤3 điểm phải sửa), ảnh FINAL 12 màn cho báo cáo.
- **`PROMPT_DIAGRAMS_DRAWIO.md`** — 6 ảnh (4 use case + 2 ERD) theo mẫu draw.io chính thức. Đã fix 6 lỗi review: repo đúng `jgraph/drawio-diagrams` + `jgraph/drawio-mcp` (5170★ — tài liệu AI chính thức: style-reference, mxfile.xsd, xml-reference); UML 2.5.1 §18.1.4 (include/extend đều nét đứt + mũi tên hở, nhãn «include»/«extend»); hướng include gốc→bị-include; **verify đếm 28 UC hiển thị** (32 − gộp UC-06+07 − loại UC-14/22/26); bỏ 3 extend trỏ UC-03 (không ảnh nào vẽ); 4 actor (Khách/Người học/GV/Admin); palette teal #0D9488 không neon; **0 dây chéo** (script intersect); drawio CLI path check; ollama list check.
- **`PROMPT_I_VIEW_QUALITY.md`** — audit + nâng cấp 36 view (bảng chuẩn 7 trục + thư viện lung linh: canvas-confetti/dotlottie/auto-animate/TresJS cân nhắc — lazy-load bắt buộc; Lighthouse ≥90 a11y/≥80 perf, axe-core 0 critical, 36/36 view có animation, 0 hardcode spacing). **13/08 cập nhật: nhúng phương pháp DESIGN.md (brand contract) từ Open Design (`nexu-io/open-design`, Apache-2.0, 30.9k⭐) — CHỈ mượn phương pháp, KHÔNG cài tool/copy token thương hiệu khác**: BƯỚC 1a viết `frontend/DESIGN.md` (9 phần: Visual Theme · Color Roles · Typography hierarchy · Component shadcn map · Layout/radius scale · Depth & Elevation = luminance stacking + border semi-transparent thay shadow dày · Do's/Don'ts · Responsive · **Agent Prompt Guide**) làm brand contract duy nhất mọi agent phải đọc trước khi sửa view; 1a/1b tách: DESIGN.md là nguồn, standard.md là bảng audit trích từ đó. **13/08 cập nhật lần 2 (vấn đề user phàn nàn: button khít chữ — đã verify 17 `<button>` raw trong views)**: trục mới "Interactive sizing & spacing" ưu tiên sửa trước (mọi nút qua Button.vue/buttonVariants, padding chuẩn md `h-10 px-4 py-2`/sm `h-9 px-3`/lg `h-11 px-8`, text cách viền ≥8px, icon-text gap ≥8px, target ≥24×24 WCAG 2.5.8, nút liền kề ≥8px), BƯỚC 2 bắt buộc đo computed padding từng button, VERIFY đếm raw `<button>` = 0 + Playwright đo 10 view; vòng Ollama thêm câu hỏi cụ thể spacing/density + điểm mục 'spacing/thoáng đãng' ≤ 3 phải sửa.
- **`PROMPT_II_VIEW_QUALITY_SOUL.md`** (MỚI 13/08, user tự viết nâng cấp) — **patch bổ sung sau I**, chạy SAU khi v1 xong: thoát "nhựa AI gen" — kill-list cliché (gradient 2 màu mặc định, hero formula, emoji icon, glassmorphism tràn lan, animation cơ giới >80% card...), **BƯỚC 0 Design Identity** (`frontend/DESIGN-IDENTITY.md` — brainstorm → tự phản biện "AI khác có ra y hệt không" → mới sửa DESIGN.md §1; motif bắt nguồn từ DSA: node-edge/mono block/Big-O), trục **Đặc trưng 0-10** thêm vào scorecard, **@vue-flow/core** cho lộ trình node-edge (signature lớn nhất, chỉ nhóm B dùng), Rive tùy chọn, lenis + canvas-confetti ĐÃ CÀI (chỉ kích hoạt). **CHẠY 3-4 SESSION SONG SONG** (chốt 13/08): Phase 0 (1 session serial: identity+token+component chung) → Phase 1 (4 nhóm A/B/C/D song song, mỗi nhóm 1 worktree + nhánh feature/view-quality-{a-d}, PR base dev, không đụng DESIGN.md/backend/DB, i18n phân vùng key) → Phase 2 (1 session tổng hợp dev-review). Bằng chứng thật: 12 ảnh `r2-fixed-01..12` (docs/work/) — banner gradient random mỗi trang, path render card phẳng thay vì graph, empty-state công thức y hệt, 🎯 dùng 2 ngữ cảnh, card không phân cấp; + 4 bug thật (header lặp home, ladder icon vỡ, dev-note Monaco lộ, label "Miễn phí tìm (20.4)").
- **`PROMPT_J_BACKEND_AUDIT.md`** — audit 5 trục backend (exception IExceptionHandler .NET 10, business logic RowVersion/idempotency/transaction, EF N+1/NoTracking/keyset, security JWT/RBAC/rate limit/CORS, migration khớp entity). **BUG-1 nhúng sẵn: job downgrade Premium KHÔNG tồn tại** (`GamificationService.cs:840-844` — hết hạn vẫn HeartsMax 30, SRS FR-10.7 yêu cầu clamp 10) — ghi chi tiết `docs/work/audit-notes-backend.md`.
- **`PROMPT_K_SEED_PROD.md`** — seed dữ liệu demo production-like (33 bảng, "app phát hành 1 tháng"): 6-8 student mới @university.edu.vn + lịch sử học (progress/submissions/quest/XP-gems-streak/achievements/inventory/favorites/feedback) + 2 lớp học (member/assignment/nộp đúng hạn-trễ-chưa nộp) — idempotent, khớp gamification rules, KHÔNG xóa dữ liệu runtime. **Kèm fix domain đăng ký (user chốt 13/08): bỏ chặn hẳn — mọi email đều đăng ký được** (xóa setting `allowed.email.domains` khỏi SeedData + bước upsert xóa setting cũ trong SeedDemoActivity; test @gmail.com register PASS; đồng bộ SDD §7.5/SRS). Đã verify DB thật 13/08: 16 bảng hoạt động = 0 dòng, Achievements 0, 6 users (3 seed + 3 rác smoke giữ nguyên), student@demo.local id=3 premium sẵn. Research chi tiết mục 8.
- **`PROMPT_L_TEACHER_REGISTER.md`** — form đăng ký giảng viên trong app (thay checkbox "Tôi là giảng viên"): form con Khoa/Bộ môn + Mã GV + Kinh nghiệm → migration 3 cột mới User (Department/StaffCode/TeacherBio nullable) → vẫn TeacherPending → Admin thấy đủ info khi duyệt. User đã loại Google Form (lệch docs, data tách rời). BẮT BUỘC worktree `D:\FPT\neww-teacher` (H có thể đang sửa RegisterView/AdminUsersView). Kèm test BE+FE + Ollama 7 tiêu chí + đồng bộ docs.
- **`PROMPT_M_FINAL_REVIEW.md`** — review TOÀN BỘ cuối trước bảo vệ (chạy sau khi H/J/K/L merge xong dev): 7 trục — (1) nhất quán code↔docs↔DB (ID FR/UC, EF pending, API↔API_REFERENCE, SCREEN_MAP↔route), (2) E2E 6 hành trình user (Playwright + Ollama 7 tiêu chí), (3) build/test toàn bộ + đối chiếu TEST_PLAN, (4) Lighthouse a11y/perf + bundle, (5) bảo mật (tổng hợp J), (6) sẵn sàng demo + checklist §17.9 chạy lại + SETUP_TODO + **kịch bản demo có fallback** (nguồn MCP: CSUN defense guide — "demo fail = fail defense"), (7) git + rủi ro bảo vệ (2FA SMTP, ngày bảo vệ, PR #1). Chỉ sửa lỗi Cao, Trung/Thấp → backlog `docs/work/final-review-2/backlog.md`. KHÔNG thêm tính năng mới. **Đã nhúng research MCP (exa 13/08 — 5 chủ đề): pre-launch QA checklist, OWASP Top 10:2025 (A01/A07/A03), Vue3+Vite 7 chiến lược + INP/LCP + WCAG 2.2, EF Core query perf, demo tips.**
- **`PROMPT_N_OPTIMIZATION.md`** — đợt tối ưu sau M: nguồn duy nhất là backlog; mỗi mục 1 task nhỏ + đo trước/sau (bắt buộc), hướng: FE (lazy route/vendor split/preload hero/INP), BE (AsNoTracking/projection/keyset pagination/caching/compression), Engine (transferable objects/canvas diff/60FPS), dùng MCP context7 + exa + chrome-devtools khi cần; KHÔNG đổi hành vi (test cũ PASS), commit `perf:`.

## 5. 📌 BÀI HỌC ĐÃ ĐÚC KẾT (bắt buộc — đã nhúng vào mọi prompt)
1. **LỖI "TASK TRẢ RỖNG" (13/08 — 4/4 lần)**: KHÔNG nhúng file >5KB vào prompt task (style-guide 21KB → subagent vượt context → trả rỗng). Prompt task CHỈ trỏ đường dẫn file. Xử lý: (1) bỏ nhúng → đường dẫn; (2) task test siêu nhỏ; (3) tách nhỏ task (1 ảnh/5-6 view/task); (4) resume session cũ — không tạo task trùng; (5) 2 lần fail → ghi FAIL + lý do.
2. **PR base `dev` KHÔNG main** (đã đóng 8 PR ma). Commit-as: backend→bao, frontend/UX→son, engine/test→thu, docs→phuc.
3. **Ollama review UI bắt buộc 7 tiêu chí** (UI: thẩm mỹ/nhất quán/rõ ràng/phản hồi trực quan · UX: luồng thao tác/tiếp cận/thỏa mãn — 1-5 điểm, ≤3 phải sửa ≤2 vòng).
4. **Task NHỎ + fresh context + explore trước + trạng thái ghi docs/work/** (skill pm-prompt-std).
5. **Không ai tự chấm bài mình**: dev viết → dev-test verify → dev-e2e (UI) → dev-review chốt APPROVE/CHANGES REQUESTED.
6. **--auto: ghi decision log TRƯỚC khi làm; lệch docs chủ ý phải có task dev-docs đồng bộ đi kèm.**

## 6. VIỆC CÒN LẠI (H/J/K/L ĐÃ MERGE XONG DEV 13/08)
1. **PROMPT_I** (view quality v1) — chạy kế tiếp (H/J đã xong — không còn chờ). Xong I → chạy **PROMPT_II** theo phase: Phase 0 (1 session: DESIGN-IDENTITY + token + component chung) → Phase 1 (4 session song song nhóm A/B/C/D, worktree + nhánh riêng `feature/view-quality-{a-d}`) → Phase 2 (1 session tổng hợp dev-review).
2. **PROMPT_K** (seed production-like + fix domain đăng ký) — có thể chạy song song ở worktree `D:\FPT\neww-seed`; cân nhắc chờ H xong các màn phụ thuộc dữ liệu (classes/leaderboard/profile) vì DB dùng chung.
3. **PROMPT_L** (form đăng ký giảng viên) — worktree `D:\FPT\neww-teacher`; đợi H xong RegisterView/AdminUsersView nếu H đang sửa.
4. **PROMPT_M** (final review toàn bộ 7 trục) — chạy SAU khi H/J/K/L merge xong dev → chỉ sửa lỗi Cao → sau đó **PROMPT_N (tối ưu theo backlog, đo trước/sau)**.
5. **MCP status 13/08**: exa (key 6e92b042... — **verify lại OK qua API call hôm nay**), context7, duckduckgo, fetch, github, playwright, chrome-devtools, memory — tất cả enabled. notebooklm ĐÃ TẮT (auth fail). Cách gọi exa từ PowerShell: `Invoke-WebRequest https://api.exa.ai/search -Headers @{"x-api-key"="6e92b042-8846-40e7-80ec-e8417c01a58e"}` + JSON body `{query,numResults,contents.text.maxCharacters}`.
6. **MERGE PLAN (13/08 — khi H/J/K/L chạy xong, tuần tự)**: (1) H ux-h-d → dev (đang chạy, đã merge b/c); (2) J backend-audit → dev (nền trước); (3) K seed-prod → dev; (4) L teacher-register → dev (cuối cùng — đụng nhiều). **Conflict nóng**: K∩L cùng AuthService.cs (khác vùng hàm, auto-merge + review); L∩H cùng RegisterView/AdminUsersView (merge H trước, rebase L, resolve); J∩L cùng ApplicationDbContextModelSnapshot.cs nếu cả 2 thêm migration (merge nhánh trước, nhánh sau rebase + migration mới). Mỗi PR: dev-test verify → dev-review APPROVE → merge dev. Sau merge hết: chạy `--seed` lần cuối (schema L + data K) → PROMPT_M → PROMPT_N → merge dev→main (PR #1, user duyệt) → 18 ảnh báo cáo + docx → SETUP_TODO (ngày bảo vệ, 2FA SMTP thật) → xóa worktree diagrams/seed/teacher/review/opt.
5. Merge dev→main (PR #1) sau khi user duyệt bản cuối.
6. 18 ảnh báo cáo: 6 sơ đồ (diagrams session) + 12 màn UI (H session — ảnh FINAL) → build lại docx pandoc (C:\Users\Administrator\AppData\Local\Pandoc\pandoc.exe) theo BAO_CAO.md.
7. Điền ngày bảo vệ/ngành bìa (SETUP_TODO mục 1-2 — cần user).
8. 2FA cần SMTP thật (đang MailHog); premium QR đã xong (G-PHU).
9. Docs đồng bộ rename UC (SRS §5.1 tên cũ vs ảnh dùng tên mới — diagram-notes.md đã ghi).
10. Xóa worktree diagrams khi xong: `git worktree remove D:\FPT\neww-diagrams`.
11. Đăng ký email: hiện chặn domain (chỉ @university.edu.vn — AuthService.cs:59-70) → prompt K sẽ bỏ chặn hẳn; đăng ký GV qua form (prompt L).

## 7. MÔI TRƯỜNG
- Backend :5000 (SQL Server Docker, seed student@demo.local/Student@123 — domain đăng ký: `university.edu.vn`), frontend :5174 (5173 bị relay Docker chiếm).
- pandoc: C:\Users\Administrator\AppData\Local\Pandoc\pandoc.exe. Ollama qwen2.5vl:3b @ localhost:11434. draw.io CLI: "C:\Program Files\draw.io\draw.io.exe" (nếu winget cài).
- GITHUB_TOKEN + EXA_API_KEY đã lưu trong ~/.config/opencode/opencode.jsonc (ngoài repo).
- **TOKEN (13/08)**: GITHUB_TOKEN đã setx user-level (gho_... — verify REST OK, mọi session MỚI có; session cũ phải mở lại). EXA_API_KEY hardcode trong opencode.jsonc. git push dùng credential manager github.com. Không có gh CLI — dùng REST API: `Invoke-RestMethod https://api.github.com/repos/Tqson3118/Visualization/pulls -Headers @{Authorization="Bearer $env:GITHUB_TOKEN";"User-Agent"="opencode"}`.

## 8. RESEARCH SEED K (13/08 — đã verify DB thật, nền cho PROMPT_K)
- **Backend THẬT đang chạy là `D:\FPT\neww\backend` (DsaVisual.sln, namespace DsaVisual.*)** — `source/VisualizationDSA*` là bản cũ/stale (KHÔNG trong git, không đụng). 33 DbSet (`AppDbContext.cs:14-48`), 22 migration, mới nhất `20260809182125_AddQuizXpGrantUniqueIndex`.
- Seeder thật: `backend/src/DsaVisual.Application/Persistence/Seed/{SeedData.cs, SeedRunner.cs(59KB), README.md}` — 9 bước idempotent, chạy bằng `dotnet run --project src/DsaVisual.Api -- --seed` (Program.cs:172-181, KHÔNG tự chạy khi start), cần env `DSA__Jwt__Secret` + `ConnectionStrings__Default` (SQL Server `DsaVisual`, sa/DsaVisual@Dev123, docker `neww-sqlserver-1`).
- **DB thật 13/08** (sqlcmd): content OK — Topics 5, Lessons 8, LessonSimulations 14, Exercises 29, Questions 76, Paths 5, Nodes 18, Quests 8, Shop 8, Settings 9. **Trống**: Achievements 0 (cả bảng), UserAchievements 0, ExerciseSubmissions 0, UserNodeProgress 0, UserProgress 1, Classes/ClassMembers/ClassAssignments 0, UserInventory 0, Favorites 0, CodeSubmissions 0, BugReports 0, LessonNotes 0. Users 6 (3 seed + 3 rác smoke `qa.debug3@`/`univ123@`/`smoke2fa...@university.edu.vn` — giữ nguyên). student@demo.local id=3: PremiumUntil 13/09/2026, HeartsMax 30, 1 sub OrderRef DSV3T1 (gp-t7).
- **Rule hệ thống phải khớp khi seed**: XP CHỈ từ quest claim (`GamificationService.cs:349-362`); Level = 1+floor(sqrt(Xp/100)) (:1030); UserQuest.Progress đặt tay (:266-345); streak eager theo LastActivityDate (:921-957) — đặt LastActivityDate = hôm nay UTC+7; node pass ⇔ Submission score == MaxScore (`ExerciseService.cs:745`); Stars = ceil(score×3/max) 0..3; leaderboard 3 tab đều sort theo User.Xp (week lọc LastActivityDate ≥ thứ Hai UTC+7) (:396-454); OrderRef = `DSV{userId}T{months}` (:665); Premium active = Status≠1 && ExpiresAt>now (:632-651); Feedback comment ≤ 200 ký tự; Class InviteCode 6 ký tự A-Z0-9; ClassAssignment CHECK lesson XOR exercise; report lớp: OnTime/Late/NotSubmitted, LaggingLearners = thiếu ≥ 2 bài (`ClassService.cs:418-507`); Achievements ConditionJson chỉ metadata — UserAchievements seed trực tiếp OK (:84-112); Notifications ĐÃ CẮT khỏi schema.
